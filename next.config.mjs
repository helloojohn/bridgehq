/** @type {import('next').NextConfig} */
const nextConfig = {
	compiler: {
		styledComponents: true,
	},
};

export default nextConfig;

// module.exports = {
// 	compiler: {
// 		styledComponents: true,
// 	},
// };